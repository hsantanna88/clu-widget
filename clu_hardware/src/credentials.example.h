// Copy this file to credentials.h and fill in your details
// credentials.h is gitignored — your secrets stay local

// Home WiFi (WPA2-PSK)
#define HOME_SSID "YOUR_WIFI_SSID"
#define HOME_PASS "YOUR_WIFI_PASS"

// Eduroam (WPA2-Enterprise) — optional, comment out if not needed
#define EDU_SSID     "eduroam"
#define EDU_USER     "user@university.edu"
#define EDU_PASS     "YOUR_PASSWORD"

// CLU server (your Mac's local IP — printed by clu --serve)
#define SERVER_IP   "192.168.1.X"
#define SERVER_PORT 8765
